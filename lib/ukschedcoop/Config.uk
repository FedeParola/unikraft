menuconfig LIBUKSCHEDCOOP
	bool "ukschedcoop: Cooperative Round-Robin scheduler"
	default y
	select LIBNOLIBC if !HAVE_LIBC
	select LIBUKSCHED

if LIBUKSCHEDCOOP
config LIBUKSCHEDCOOP_HALT_POLL_NS
	int "Polling interval before halting a vCPU (ns)"
	default 200000
endif
